# The Following is required to allow Spark to be able to manage Driver and Executor pods
# Make sure namespace `aiks` is already created.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: spark
  namespace: aiks
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: spark-role
  namespace: aiks
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log", "services", "configmaps", "persistentvolumeclaims"]
  verbs: ["create", "get", "list", "watch", "delete", "deletecollection", "patch", "update"]
- apiGroups: [""]
  resources: ["events"]
  verbs: ["create", "patch", "update"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: spark-role-binding
  namespace: aiks
subjects:
- kind: ServiceAccount
  name: spark
  namespace: aiks
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: spark-role